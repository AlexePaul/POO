#include "../include/doubleLL.h"

using namespace std;

template<typename type>
doubleLL<type>::doubleLL(){
	this->size = 0;
	this->front = NULL;
	this->back = NULL;
}

template<typename type>
doubleLL<type>::push_back(type element){
	if(this->front == NULL){
		
	}
}